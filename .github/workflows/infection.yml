name: Infection

# Mutation testing on every PR. Runs in `--git-diff-lines` mode so
# only mutations in changed code are evaluated — full-suite mutation
# runs would take >30min on a tree-heavy package, but the diff mode
# is fast enough to gate PRs.
#
# Reports MSI (Mutation Score Indicator) and Covered MSI to the PR
# annotations. Thresholds in infection.json5 are at 0 initially —
# ratchet them up once a baseline lands on master.

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  mutation:
    name: Mutation testing
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v6
        with:
          # Infection's --git-diff-base needs the base branch fetched.
          fetch-depth: 0

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, pdo_sqlite
          # pcov is dramatically faster than xdebug for coverage and
          # Infection only needs line coverage, not full debug info.
          coverage: pcov
          tools: composer:v2

      - name: Configure Composer authentication
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: composer config --global --auth github-oauth.github.com "$GH_TOKEN"

      - name: Cache Composer dependencies
        uses: actions/cache@v5
        with:
          path: ~/.composer/cache/files
          key: composer-infection-${{ hashFiles('composer.json') }}
          restore-keys: composer-infection-

      # Infection 0.29.x maxes out at symfony/console ^7, but Laravel 13
      # pulls symfony/console ^8. Pin the mutation-testing job to
      # Laravel 12 so Infection resolves — `src/` is identical across
      # Laravel versions, so this doesn't change what is mutated.
      - name: Pin Laravel 12 for Infection compat
        run: |
          composer require --dev --no-update \
            "laravel/framework:^12.0" \
            "orchestra/testbench:^10.0"

      - name: Install dependencies (with Infection)
        run: |
          composer require --dev --no-update "infection/infection:^0.29"
          composer config --no-plugins allow-plugins.infection/extension-installer true
          composer update --prefer-dist --no-interaction --no-progress

      - name: Run Infection (diff mode)
        env:
          # Tells Infection which lines changed in this PR. The
          # ${{ github.base_ref }} is the target branch name (e.g.
          # `master`); origin/<ref> is the remote tracking ref after
          # the full-depth checkout above.
          INFECTION_BADGE_API_KEY: ''
        run: |
          vendor/bin/infection \
            --git-diff-lines \
            --git-diff-base="origin/${{ github.base_ref }}" \
            --logger-github \
            --no-progress \
            --no-interaction \
            --threads=$(nproc)
